qpid-xsink - send and receive messages for MRG queues
usage: qpid-xsink {BROKER CONNECTIONOPTION SESSIONOPTIONS]

       where BROKER is the address of a qpid broker (optional)
             CONNECTIONOPTIONS are the qpid broker connection options passed to the broker
             SESSIONOPTIONS are the qpid broker session options passed to the broker
